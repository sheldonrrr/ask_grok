# /version-up — Alias for `/upgrade-version`

This command is an **alias**. Follow `.cursor/commands/upgrade-version.md` exactly (same steps, same version/docs rules).

If the user wrote `/version-up 1.5.2`, treat it as `/upgrade-version 1.5.2`.
