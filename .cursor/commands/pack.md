# /pack — Alias for `/build`

This command is an **alias**. Follow `.cursor/commands/build.md` exactly (same `scripts/package.sh` steps, same zip checks).

If the user wrote `/pack`, treat it as `/build`.
