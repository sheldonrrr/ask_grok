# Ask AI Plugin - Version 1.4.0 Release Notes

[b]New Features (v1.4.0)[/b]
[list]
[*][b]Free Tier Channel (Nvidia AI Free)[/b] - Added a completely free AI channel that works out of the box without any configuration. New users can start asking questions immediately using Nvidia AI Free, which is now the default AI provider.
[*][b]Zero Configuration Setup[/b] - Implemented "configuration equals minimum viable product" logic. The plugin is now fully functional immediately after installation with no setup required.
[/list]


[b]Bug Fixes (v1.4.0)[/b]
[list]
[*][b]Fixed AI Deletion Bug[/b] - Resolved issues that occurred when users deleted configured AI providers.
[/list]

---

## GitHub Release Notes (v1.4.0)

### New Features
- **Free Tier Channel (Nvidia AI Free)** - Added a completely free AI channel that works out of the box without any configuration. New users can start asking questions immediately using Nvidia AI Free, which is now the default AI provider.
- **Zero Configuration Setup** - Implemented "configuration equals minimum viable product" logic. The plugin is now fully functional immediately after installation with no setup required.

### Bug Fixes
- **Fixed AI Deletion Bug** - Resolved issues that occurred when users deleted configured AI providers.
