# Ask AI Plugin - Version 1.4.3 Release Notes

[b]Bug Fixes (v1.4.3)[/b]
[list]
[*][b]Windows Compatibility Fix[/b] - Fixed module import error that prevented the plugin from loading on Windows
[*][b]Improved Error Handling[/b] - When opening books with problematic file paths (e.g., invalid characters or locked files), the plugin now gracefully skips those books instead of failing completely
[/list]

[b]Improvements (v1.4.3)[/b]
[list]
[*][b]Keyboard Shortcut Change[/b] - Changed Random Question shortcut from Ctrl+R to Ctrl+Shift+R to avoid conflict with Calibre's built-in Restart shortcut
[*][b]Better Error Messages[/b] - Error dialogs now display in the user's selected language instead of hardcoded Chinese
[/list]

---

## GitHub Release Notes (v1.4.3)

### Bug Fixes
- **Windows Compatibility Fix** - Fixed module import error that prevented the plugin from loading on Windows
- **Improved Error Handling** - When opening books with problematic file paths (e.g., invalid characters or locked files), the plugin now gracefully skips those books instead of failing completely

### Improvements
- **Keyboard Shortcut Change** - Changed Random Question shortcut from Ctrl+R to Ctrl+Shift+R to avoid conflict with Calibre's built-in Restart shortcut
- **Better Error Messages** - Error dialogs now display in the user's selected language instead of hardcoded Chinese
